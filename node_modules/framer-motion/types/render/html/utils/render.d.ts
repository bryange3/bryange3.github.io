import { IProjectionNode } from "../../../projection/node/types";
import { HTMLRenderState } from "../types";
export declare function renderHTML(element: HTMLElement, { style, vars }: HTMLRenderState, projection?: IProjectionNode): void;
