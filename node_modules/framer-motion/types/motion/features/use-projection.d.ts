import { VisualElement } from "../../render/types";
import { MotionProps } from "../types";
import { InitialPromotionConfig } from "context/SwitchLayoutGroupContext";
export declare function useProjection(projectionId: number | undefined, { layoutId, layout, drag, dragConstraints, shouldMeasureScroll, }: MotionProps, visualElement?: VisualElement, initialPromotionConfig?: InitialPromotionConfig): void;
